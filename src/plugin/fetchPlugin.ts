/** Main fetch plugin implementation*/
export const fetchPlugin = async (
  details: {
    apiUrl: string;
    method: string;
    headers: any;
  },
  completeContext: any,
): Promise<any> => {
  //do the fetch;
  return 90;
};
